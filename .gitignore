node_modules/
dist/
build/
coverage/
.turbo/
.cache/
.DS_Store
*.log
.env
.env.*
!.env.example

# Generated sticker-sheet token artifacts — rebuilt on every CLI run.
packages/sticker-sheet-*/src/generated/

# Personal toolchain pin (asdf / mise).
.tool-versions

# Personal scratch palettes — keep the bundled examples below tracked, but
# any new example directory is local-only by default.
examples/*/
!examples/basic/
!examples/portable-demo/
!examples/portfolio-dark/
